import React from "react"

import * as loadingSpinnerStyles from "./loadingSpinner.module.scss"

const LoadingSpinner = () => {
    return <span className={loadingSpinnerStyles.loader}></span>
}

export default LoadingSpinner
